{
	instances = {
		byid = BodyReader(
			field_key = "^[0-9|A-Z|a-z|_]+$",
			field_value = ".*",
			maperrors = true
		) {
			key_failure->Template(f"keyfailure.html");
			form->Module(f"view.conf");
			successful->SetSetting() {
				successful->Module(f"view.conf", message = "Setting set.");
				failure->Module(f"view.conf");
			};
		};
	};
}